## Testcontainers configuration for Windows + Docker Desktop (WSL2 engine)
## Tells Testcontainers to use the dockerDesktopLinuxEngine named pipe
## instead of the default docker_engine pipe.
docker.client.strategy=org.testcontainers.dockerclient.NpipeSocketClientProviderStrategy
